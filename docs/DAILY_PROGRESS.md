# Daily Progress

**Area:** Change log · **Last updated:** 2026-08-10

> Append-only, dated log of every change and why it was made. **Standing rule:** every change to this repo updates the relevant `docs/*.md` file AND adds an entry here — newest first.

---

## 2026-08-12 — Voice: actionable start errors + mic-permission preflight

- **`[start-failed]` on the live site was a dead end** — no cause, no fix. Fixed the machine: **mic permission is now requested (`getUserMedia`) BEFORE `recognition.start()`** (it was racing — the analyser's `getUserMedia` ran async/unawaited with errors swallowed), so a denial surfaces cleanly instead of an opaque `start()` throw. Errors now carry the **`DOMException` name + message**, and are mapped: `NotAllowedError`/`SecurityError` → *"Microphone blocked — click the icon in the address bar and allow access"*; `NotFoundError`/`NotReadableError` → no-device; else → *"Couldn't start the microphone — close other apps/tabs using it"*. Permission/device errors are no longer retried (only `InvalidStateError` is). Tests: +5 machine (preflight resolve/reject mapping, start-throw name), +4 console (actionable text + name surfaced + preflight configured). Re-verified the full loop in real Edge.

## 2026-08-12 — Voice deploy fix (routing + notice spam)

- **`/speech.js` and `/voice.js` 404'd on Vercel** (browser refused the `text/plain` 404 body → `window.M2Voice`/`M2Speech` undefined → voice dead). Root cause: `vercel.json` had explicit rewrites for `/firebase-config.js` and `/fx.js` but none for the two files added later. Added matching rewrites so both resolve to `/app/*.js` with a JavaScript MIME type. Verified with **`scripts/verify-deployed-routing.js`** — a static server honoring `vercel.json`'s rewrites, driven in real Edge: both scripts return `text/javascript` (200), the modules execute, and the loop runs `listening→…→idle`. Also documented in FRONTEND.md that every root-level asset needs a rewrite.
- **"Voice isn't supported" spammed the chat** on repeated mic taps — now guarded to appear once per session. Test added.

## 2026-08-12 — TASK_M2_VOICE_FIX (make the voice loop actually work in a real browser)

Voice — the core "talk to your server" feature — was **stuck** in a real browser: tap the mic and the conversation never completes. The jsdom tests passed, so this was a mock/reality gap. Rebuilt the whole thing against how the Web Speech API *actually* behaves and verified it in real Edge. Suite 226→242.

- **Root cause.** The loop relied on `speechSynthesis` firing an utterance's `onend` to relisten, but in real Chromium `onend` frequently **never fires** (long text / cancel races / no audio device) and there was **no watchdog** — so the machine stuck in "speaking" forever and the mic never reopened. Two secondary hangs: `recognition.start()` swallowed `InvalidStateError` while the UI already showed "listening" (recognition wasn't actually running), and **no state had a timeout**.
- **M1 — instrument** (this batch): a **Voice debug** setting (off by default) with a live panel (state, last event, elapsed, rec language, voices loaded, support flags, last error) + gated console logging of every Web Speech event; every recognition/synthesis error now shows in chat with its code — never a silent hang.
- **M2 — one explicit state machine** (`app/voice.js`, `window.M2Voice`): `idle→listening→transcribing→thinking→speaking→(listening|idle)`, **every state timed out to a safe landing**. Branches on whether a *final result* arrived (not `onend`); guards every `start()` with abort-then-retry (suppressing the abort's `onend`); never listens while speaking (`cancelAndWait` + settle); idempotent `stop()`. `app/speech.js` is now a driver that **guarantees `onDone`** (onend | error | **watchdog**), with keepalive + async `cancelAndWait` + `ensureVoices`. `app/index.html` rewired to drive the machine with distinct listening/thinking/speaking visuals. Tests: 16 machine + 10 driver + 10 console glue.
- **M3 — real-browser verification** (`scripts/browser-voice-check.js`, `npm run voice:browser`): drives the SHIPPED page in real Edge over CDP with fake-media flags. Observed for real: synthesis completing via **watchdog at ~3.2s** (onend never fired — the root cause, now survived), the full loop `listening→transcribing→thinking→speaking→listening`, barge-in returning to listening, and stop from listening/mid-turn + double-stop all → idle. STT audio→text has no backend in headless automation, so a final transcript is injected through the real result path (disclosed).
- **M4 — real-world failures:** insecure-context + unsupported-browser messages on mic tap (text stays fully working); one-time "no Arabic voice" notice (degrades, never throws); hidden tab stops the loop; network/timeout errors surface visibly and land safe. Browser-support matrix documented in FRONTEND.md.
- **M5 — feel:** thinking is now visually distinct from speaking (slower, dimmer pulse); echo-cancelled audio **barge-in**; a "still working…" latency hint after ~2.2s; Stop is present and works in every active state.
- **Decisions on the human's behalf:** kept the machine framework-free and injectable (fake clock/recognition/synth) so every transition + timeout is deterministic in tests *and* verifiable in a real browser; barge-in is best-effort (documented — headset ideal, open speakers may false-trigger); STT-in-automation gap disclosed rather than papered over with a mock.
- **Unverifiable without a human:** a live microphone permission **denial** (fake-media auto-grants it in automation — the not-allowed→visible-error mapping is unit-tested; a person must click "Block" on the mic prompt to see the toast); real spoken-audio quality and installed OS voices; iOS Safari behaviour (no device available here). Exactly what to click is in the final report.
- **npm test 242/242, smoke 15/15, `npm run voice:browser` PASS in Edge.**

## 2026-08-12 — TASK_M2_VOICE_ASK (make Ask talk, and talk well)

Five milestones fixing three Ask-mode defects and adding spoken, hands-free conversation. TDD throughout; suite 185→226.

- **M1 — purge the old product name** (8822bdc): the old name still lived in a live Ask system prompt ("I am …"), plus docs, UI copy, and internal identifiers. Replaced it everywhere with **M2** — product/user-facing strings, the bridge's inactive-plan notice, and internal ids (auth-ready event, firebase global, bridge resource, emulator project, package name, smoke email). New `tests/no-old-name.test.js` scans every git-tracked file and fails on any reappearance.
- **M2 — reply language follows the user** (this batch): `lib/lang.js` detects Arabic vs Latin per message (dominant script wins; mixed/letter-less → tenant default). `/api/command` threads the detected language through **both** modes and `backend/actions.friendlyMessage` is now bilingual, so "repair my car" confirms in English and «صلّح سيارتي» in Arabic. The client sends no language, so the UI toggle can't affect replies. Tests: detector (6) + endpoint EN/AR × Ask/Run + ambiguous + toggle-independence (7).
- **M3 — an Ask persona that explains** (with M2): `lib/ask-persona.js` — one product-knowledge source shared by the AI system prompt and the no-key fallback. Friendly operator voice: explains "how does this work", is concrete about *this* server (framework/inventory/jobs from the latest scan + `allowedActions`), brief, suggests a next step, knows the product (Run, scanner, Whitelist Officer), and is written natively in Arabic. The deterministic fallback is genuinely helpful, not a one-liner. Tests (7).
- **M4 — M2 speaks (browser TTS)** (this batch): `app/speech.js` (`window.M2Speech`) on `speechSynthesis`, behind a swappable interface (a premium voice provider can drop in later). Auto-speaks only voice-sourced replies; voice matches the reply language; never overlaps; strips markdown + caps length; handles async `voiceschanged`. Console: a *Voice replies* setting (off / when-I-speak / on), a per-message speaker button, a gently-pulsing speaking orb + Stop, and speech cancelled on new message / recording / mode-switch / navigation. Tests: engine (9) + wiring (5).
- **M5 — continuous conversation** (this batch): a voice-started exchange runs hands-free (listen → think → speak → listen) until stopped. Safety caps — turn cap, silent turn, any error, or Stop — mean the mic is never left open; a typed message never starts the loop. Tests (6).
- **Decisions on the human's behalf:** kept internal identifiers renamed too (not just user-facing) so "M2" is the single name everywhere, and added a guard test so it can't regress; browser `speechSynthesis` only (no paid TTS) — noted in FRONTEND.md that a premium provider swaps in behind `M2Speech`; the reply-language decision is server-side (the client never sends a language) so the UI toggle is provably independent; the hands-free loop defaults to *when I speak* and hard-caps turns/silence/errors.
- **Unverifiable:** live Gemini Ask replies (tests exercise the deterministic fallback + the prompt builder; a real key uses the AI path); real browser speech synthesis/recognition and installed OS voices (jsdom mocks stand in — the daemon-based browser screenshot path is still broken in this environment).
- **npm test 226/226, smoke 15/15.**

## 2026-08-12

- **TASK_M2_CONCIERGE — The Concierge (all 8 milestones, TDD, suite 139→185)** — an in-game AI that onboards new players to fight FiveM's retention death spiral. Greets on join, asks what character they want, drops a waypoint to a matching destination, points out a nearby player, checks back in ~5 min — EN/AR. **Notify-only by construction:** a closed action set of exactly `send_message` / `show_menu` / `set_waypoint`, enforced in the AI schema, the message layer, the runtime, and the bridge Lua (a static grep fails the build if any write/spawn/teleport/give primitive appears). Reuses the scanner report, the pay-gate, the bridge auth, the error envelope, and the design system. Works with **no AI key** via a deterministic per-phase fallback.
  - **M1 config** (6b93524): `lib/concierge/config.js` defaults (disabled by default) + `validateConfig` (tone/languages/check-in 60–1800/retention 1–365, AR-base required); Firestore session/event/purge helpers; GET/POST `/api/concierge/config`.
  - **M2 session** (7325e61): `lib/concierge/session.js` state machine (greet→choose→guide→await_checkin→checkin→done), ≤8-message + ≤30-min caps, serialize/restore, `shouldOnboard` returning-player logic.
  - **M3 recommend** (93d412b): `lib/concierge/recommend.js` reads the read-only scanner report for real on-server destinations, bilingual `KNOWN` fallback, `pickNearbyPlayer`.
  - **M4 message layer + AI** (65424e0): `lib/concierge/messages.js` closed action set + sanitize-through-closed + deterministic fallbacks; `personality.js`/`brain.js`; `providers/gemini.js` `conciergeReply` forced function calling (three-verb enum, 300 tokens).
  - **M5 bridge integration** (this batch): `api/concierge/_event` + `_reply` (bridge auth + gate), `lib/concierge/runtime.js` (session persist + funnel events + closed-set belt), `fivem-bridge/concierge.lua` (notify-only, no new ports). Mock-bridge e2e + static LUA grep.
  - **M6 analytics** (this batch): `lib/concierge/analytics.js` pure funnel/retention/arrivals-by-day/question-theme aggregation over the append-only events.
  - **M7 owner dashboard** (dbf37d6): `api/concierge/_stats.js` + the dashboard Concierge card — enable/tone/check-in setup, live funnel, retention tiles, arrivals-over-time bars, ranked themes; EN/AR RTL, 380px, empty state; jsdom render tests.
  - **M8 docs** (this commit): new docs/CONCIERGE.md + API/ENGINEERING/DATABASE/SECURITY/FRONTEND/UIUX/TESTING synced; showcase 185 tests + Concierge roadmap row.
  - **Decisions on the human's behalf:** disabled-by-default (opt-in per server); deterministic no-key fallback so onboarding works before any Gemini key; player chat relayed for a reply then reduced to a coarse theme and never stored verbatim; kept it to one Vercel function via the `[action].js` dispatcher (11/12 on Hobby).
  - **Unverifiable:** live Gemini replies (tests use the deterministic fallback); a real in-game FiveM round-trip (a mock bridge stands in; the Lua is static-analysed, not run in-game); a browser screenshot of the dashboard section (chrome-devtools daemon still broken — jsdom render tests of the shipped code stand in, EN+AR).
  - **npm test 185/185, smoke 15/15.**

- **TASK_M2_WHITELIST — the Whitelist Officer (all 6 milestones, TDD, suite 88→139)** — the whitelist form replaced by an AI interview. Additive, needs no server access, demoable today.
  - **M1 config** (6171654): `lib/whitelist/config.js` bilingual defaults + validation (rejects empty/AR-missing/over-cap sets, threshold sanity), `publicView` projection; stable unique slugs (`whitelistSlugs/{slug}→uid`); GET/POST `/api/whitelist/config`.
  - **M2 interview** (3894755): `lib/whitelist/interview.js` state machine — ask → injected judge → follow-up (≤2) → next → done; EN/AR; per-answer + turn caps; serialize/restore for resume-after-drop. Judge injected (deterministic in tests, provider-backed in prod); a judge failure never traps the applicant.
  - **M3 scoring** (3aeeea1): `lib/whitelist/score.js` — evidence-mandatory validation (evidence-free scores rejected), language-blind deterministic flags (copy-paste/hostile/dodge/underage) that run regardless of the model, confidence-gated `decide()` (auto only on owner thresholds + high confidence + no blocking flag), offline evidence-backed fallback, and the **bias guard** (equivalent AR/EN → identical treatment).
  - **M4 public flow** (747581a): `/api/apply/{config,start,answer,submit,resume}` — unauthenticated, per-IP throttled, one-per-identity, resume-token-hashed, appId→uid via a private index so the uid never leaks; `app/apply.html` conversational page (design system, EN/AR, voice, localStorage resume) at `/apply/{slug}`.
  - **M5 owner review** (003fd46): `/api/whitelist/{applications,decide,stats,test-webhook}` (verified+gate, cross-tenant-scoped, records decidedBy/At, delete-application); dashboard Whitelist card — Queue/Setup tabs, stats strip, ranked queue, detail (gauge + per-criterion evidence + transcript + decisions), setup (link, editable criteria/questions, Discord webhook + test).
  - **M6 Discord + provider + docs** (this commit): `lib/notify/discord.js` webhook (new-submission card, decision post, test-send; safe unconfigured); `providers/gemini.js` `whitelistJudge`/`whitelistScore` forced-schema calls with the bias instruction (offline fallback is what tests exercise). New docs/WHITELIST.md + API/ENGINEERING/DATABASE/SECURITY/FRONTEND/UIUX/TESTING synced; showcase 139 tests + Whitelist roadmap row.
  - **Decisions on the human's behalf:** deterministic flag layer + offline fallback scorer so the whole product works with no Gemini key (BYOK-with-fallback, like the console) — the AI path improves quality but isn't required; auto-decide stays OFF by default (recommend-only) per the rules; identity dedup rotates the resume token on re-start (a public whitelist identity like a Discord name isn't a secret — noted as an accepted, low-risk trade-off).
  - **Unverifiable:** live Gemini judging/scoring (tests use the deterministic fallback); a browser screenshot of the dashboard/apply pages (chrome-devtools daemon still broken — jsdom render tests of the real shipped code stand in, EN+AR); real Discord delivery (unit-tested interface only).
  - **npm test 139/139, smoke 15/15.**
- **TASK_M2_SCANNER — the read-only Server Scanner (all 6 milestones, TDD, suite 48→88)** — the shared foundation for the Doctor and the AI Installer: M2 can now read a customer's FiveM server and understand it, without ever writing/moving/deleting a byte.
  - **M1 access layer** (`lib/serverAccess/`, commit 2daa2ad): one read-only adapter contract (listFiles/readFile/stat/exists/destroy) over zip-upload (in-memory, no temp files), scan-pack (client-built JSON — the live path), directory (fixtures), bridge-payload, and an ftp *stub* (credential storage left to the human). Safety proven: entry/file/total-byte caps, text-only reads (binaries listed but never loaded), zip-slip dropped+recorded, symlinks skipped. Four fixture servers built (qbcore-clean, esx-clean, broken w/ 13 planted faults, ambiguous).
  - **M2 parse+model** (2df2513): parseManifest (both dependency forms, never executed), parseServerCfg (ordered ensures with exec recursion + loop guard), buildResourceGraph → serverModel (started/order cross-ref, ghosts, dead weight, double-nesting, missing manifests, escrow marker, stream bytes counted without reading).
  - **M3 detection** (8ab0a92): framework (QBCore/QBox/ESX+version, else **unknown** — the ambiguous fixture must not be guessed), inventory (winner + candidates), deps, jobs (grade counts), items — all with evidence + confidence.
  - **M4 checks+report** (f98e72d): 8 pluggable checks (duplicates, missingDeps, loadOrder, structure, luaSyntax, riskSignals, performance, deadWeight) + ranked weighted health score with plain-language EN/AR verdict. Broken fixture surfaces every planted fault; clean fixtures stay clean (finding the qbcore fixture's own real load-order bug in the process — exec-before-dep — which validated the check).
  - **M5 API+bridge** (bc7128f): POST /api/scan + GET /api/scan-status (verified-auth + pay-gate + per-tenant rate limit + envelope), stored under tenants/{uid}/scans/{scanId} — derived report only, NEVER raw source (findings carry locations; a storage-time sanitizer redacts secret-shaped evidence). fivem-bridge/scan.lua adds read-only scan_* commands; a test statically asserts the whole bridge has no SaveResourceFile/io.write/os.remove/rename/execute/popen.
  - **M6 dashboard + docs** (9ad83db): Server Report card (folder picker → client-built text-only scan-pack → health gauge + identity + filterable findings + resource table + export + history; design-system, EN/AR RTL, dark/light, 380px). New docs/SCANNER.md + API/ENGINEERING/DATABASE/SECURITY/TESTING/FRONTEND/UIUX/GOAL synced; showcase 88 tests + Scanner roadmap row.
  - **Decisions on the human's behalf:** folder-picker (builds a scan-pack in the browser) chosen over server-side ZIP upload — no zip lib client-side, binaries never leave the machine, same API. Upload scans run synchronously (complete in-request) rather than async-polled — correct for in-memory packs. adm-zip + jsdom added as deps.
  - **Unverifiable:** the live browser screenshot of the Server Report — the `chrome-devtools` CLI daemon broke after a forced 1.7.0 self-update (named-pipe ENOENT; 1.6.0 rollback didn't revive it). Covered instead by `tests/scanner-ui.test.js`, which drives the ACTUAL shipped dashboard render in jsdom (EN/AR/RTL, clean/broken, filter, no-secret-leak) — a durable regression test, not a one-off image.

## 2026-08-11

- **Prod incident: deployed site called the Firebase EMULATOR (127.0.0.1:9099)** — the local-testing flip of `app/firebase-config.js` (`useEmulator:true`, deliberately left uncommitted) got committed and pushed from the working tree by the owner's Git client, so the live page tried to reach an emulator that only exists on the dev machine (prod CSP correctly blocked it — that was the visible error). Two-layer fix: (1) **hostname gate** in both pages — `USE_EMU = flag && location.hostname ∈ {localhost,127.0.0.1,[::1]}` — a deployed page can now NEVER call the emulator even if the flag ships enabled again; (2) `useEmulator:false` committed as the canonical state. Production CSP already allows `identitytoolkit.googleapis.com` + `securetoken.googleapis.com` (verified in vercel.json), so live Firebase Auth needs no CSP change. Live sign-in/reset verification still blocked by Vercel Deployment Protection (site 302s to SSO — owner toggle pending). Lesson recorded: never leave environment-behavior flips uncommitted in the working tree; gate them by environment instead.
- **Professional auth: mandatory email verification + password reset + signup throttle (TDD, 48/48 + smoke 15/15)** — server-enforced verification gate: `requireVerifiedUser` 403s (`EMAIL_UNVERIFIED`, new envelope code) on every human endpoint until the token's `email_verified` claim is true — unskippable client-side, and a stale pre-verification token stays blocked because the claim lives in the token (test-proven, including the verify→refresh unlock). Both pages got a branded verify screen (resend with 60s cooldown, "I've verified — continue" that reloads the user + force-refreshes the token, switch-account) and a "Forgot password?" flow with neutral anti-enumeration wording; verification email sent via the Firebase web SDK on signup. `/api/signup` hardened: per-IP fixed-window throttle (`SIGNUP_RATE_LIMIT_PER_HOUR`, default 20/h, IPs stored as SHA-256 hashes in `rl_ip` with TTL-ready `expireAt`). The suite itself tripped the new throttle (shared emulator, ~35 signups from 127.0.0.1) — fixed with unique synthetic IPs per test signup; suite now 48 tests. E2E with a REAL emailed link (emulator oobCode): signup → locked verify screen → visit link → continue → console unlocks. Owner's production account (nettt420@gmail.com) marked verified via Admin SDK so the deploy can't lock them out. Recommended console toggle documented: Email enumeration protection ON. Docs: API/AUTH/SECURITY/TESTING/README/.env.example synced; showcase 63/63 checks.
- **Voice orb refined ("the wave is too much")** — the raw mic-to-distortion mapping felt aggressive, so the voice moment was redesigned around restraint: fx.js orbs gained an attack/release envelope (fast react, slow relax — the orb *breathes* with speech instead of jittering), a `hueShift` option (the orb warms toward pink while you speak, cooling back in silence), and speech-speed-scaled rotation; overlay intensity dropped .7→.32 with no level boost and a softer scale pulse; the 26-bar wave became a symmetric center-out bloom with tapered edges. One-frame proof: spiky star-blob → soft undulating ring. Second pass ("smooth and little slow"): per-bar temporal smoothing (glide up .22 / lazy fall .10) + .22s eased CSS transitions, orb `timeScale` option with the listening orb at .75× (slower noise morph/wobble), lazier envelope, gentler rotation.
- **WebGL voice orb + full-bleed hero stripe grid (React Bits, ported to vanilla)** — the requested `<Orb />` and `<RippleGrid />` React components can't run as-is (no React, no bundler, CSP blocks CDNs), so their shaders were ported verbatim into dependency-free `app/fx.js` (~60 lines of raw WebGL replace `ogl`; served same-origin, `/fx.js` rewrite added). Final placement after two owner corrections (first draft stacked orb+grid in the hero AND left the CSS halo/ring under the overlay blob — rule learned: **one effect per place, orb = voice only, grid = hero only**): the listening overlay's orb is WebGL with mic level driving shader distortion + spin (`setLevel` from the analyser loop) — the "wave when I talk" effect — always dark-style because a light shader background paints an opaque square that can't match the blurred backdrop; the hero keeps its ORIGINAL layout (CSS mini-orb restored, aurora removed) over a full-bleed subtle lime ripple grid. Two port extensions were needed for full-bleed: soft exponential vignette (upstream formula hard-clips to zero past half the container width) and `fadeDistance<=0` disabling the center-spotlight fade. Theme-aware, hidden containers skip rendering, DPR≤2, reduced-motion/no-WebGL → CSS fallbacks. Verified over 7 screenshot iterations at 1200px/390px, dark/light, simulated-voice overlay.
## 2026-08-10

- **Site demo: realistic phone mockup** — replaced the flat card mockup in `app/index.html`'s demo section with the improved device frame from the owner's `m2_phone_mockup.html`: gradient metal body, side + power buttons, dynamic island, status bar, app header with live Connected pill, animated AR/EN chat with flash-through typing indicators, input dock + home bar. Integrated on the existing tokens (screen themes light/dark automatically, hardware stays dark) and scoped under `.mockup` because the source file's class names (`.dock`, `.din`, `.mic`, `.typ`, `.mk`, `.bd`) collide with the live console's — `.bd` badge renamed `.mbd` (it clashed with the sheet backdrop). Verified in a driven browser: 390px (no horizontal overflow, device 290px) and 1200px two-column, dark + light. Source file deleted after absorption (it was untracked and would have shipped as a stray public page).
- **M2_POLISH review round (7-angle code review → 14 fixes)** — a multi-angle review of the whole task diff surfaced real issues, all fixed and re-proven (suite now 45/45, smoke 15/15): JSON `null`/array/number bodies crashed handlers to 500 → normalized to `{}` in `readJson` (+ regression test); typo'd `RATE_LIMIT_PER_MIN` (NaN) silently disabled the limiter → clamped fallback; signup orphaned the email forever if the tenant write failed after user creation → auth-user rollback; dev-server path containment used a bare `startsWith` prefix (a `docs-x/` sibling would be servable) → separator-safe resolve check; `/api/health` now 503s when Firestore is down (real signal for monitors + the test harness), tolerates a malformed service account instead of crashing at require, and accepts HEAD; uncleared 2.5s health timer; `/api/command` cost 2 tenant reads per request → the rate-limit transaction IS the tenant read now; bridge poll serialized the liveness stamp before the drain → parallel; dashboard hidden-tab polling burned ~80 reads/hr → skipped while hidden; console fail-open on broken `/api/tenant/me` (fabricated active session) → fail-closed "error" path; shared `stampMs` normalizer replaced two divergent timestamp decoders; dead code removed (broken test helper, unreachable auth branch, misleading env comment); Backend-URL sheet hint now mentions the `ALLOWED_ORIGIN` requirement. Deferred as known cleanups (structural, not bugs): shared client auth.js for the two pages, dev-server deriving CSP/rewrites from vercel.json, endpoint()-level auth prologue, auto-walked dev API routes, client/server validation constants.
- **M2_POLISH M6 (docs = reality)** — new [API.md](API.md) (every endpoint, the 10-code error envelope, headers, CORS policy, test seam) and [TESTING.md](TESTING.md) (how to run, what each of the 44 tests proves, TDD conventions). [UIUX.md](UIUX.md) rewritten as the actual style guide (tokens table, type, orb recipe, full component inventory, RTL/motion/interaction rules — a new page is buildable from it alone). AUTH/ENGINEERING/DATABASE/SECURITY/FRONTEND/GOAL updated for open-access signup, session persistence, envelope, rate limit, new tenant fields, hardening walls, npm test. README: current run instructions + env vars (`RATE_LIMIT_PER_MIN`, `ALLOWED_ORIGIN` in `.env.example` too). Showcase `docs/index.html`: stats now 59/59 automated checks, "Hardening & proof" roadmap row done, ring at 30%. Deleted dead `app/app.js`, `app/style.css`, `app/voice.js`.
- **M2_POLISH M3 (every failure path triggered deliberately, none dead-ends)** — console offline → "Offline" pill + "check your connection, try again"; dashboard backend-dead → sticky banner with working Retry (recovers to 4/4); 401 → one silent token-refresh retry → sign-out + "Session expired — sign in again"; 402 → dashboard Inactive pill + contact-to-activate copy, console locked chip + "View plan →" linking `/dashboard#plan`; no-AI-key → console notice → dashboard; provider outage → keyword fallback answers, internals never reach the client (suite-proven), real error in structured server logs; bridge-never-polled → "Not connected yet — finish step 3"; mic no-speech / denied / unsupported → three distinct translated feed messages (handler-level triggers). All states EN+AR.
- **M2_POLISH M1 (dashboard rebuilt in the M2 design system)** — `app/dashboard.html` rewritten from scratch on the exact index.html tokens (orb, Space Grotesk, lime/violet/pink, dark+light, EN/AR RTL, safe-areas, 16px inputs). Hero is the **4-step setup checklist** (account ✓ / AI key / server connected / first command) with animated progress bar, pulsing "next" step, and tap-to-scroll to the matching card; state computed from `/api/tenant/me` (hasKey, lastPolledAt, firstCommandAt). AI card: Gemini active, Claude "Soon", "Key saved · encrypted" state, key never echoed. Server card: masked token with reveal/copy, ready-made server.cfg copy, **inline rotate confirm** (no native dialogs), live status dot with relative "last seen". Plan card: open-access active copy + contact-to-activate inactive state (anchor `#plan` — console 402 links here). Header: mark, tenant name, plan pill, theme/lang toggles, sign out. Session persistence + 401-refresh-retry + offline banner with Retry. Theme/lang now persist in localStorage. E2E at 380px: signup → key (2/4) → real bridge poll (3/4, green Connected) → console command via persisted session → back → **4/4**; rotation kills old token live (401); RTL + light theme screenshots verified.
- **M2_POLISH M2 (console production wiring)** — `app/index.html`: session persistence (Firebase `onAuthStateChanged` → `m2-auth-ready` event → reload lands signed-in users in the console, signed-out on the site — E2E-verified both ways); `authedFetch` gives every API call a fresh ID token with ONE forced-refresh retry on 401 before clean sign-out + translated "session expired"; console boot pulls `/api/tenant/me` (name, active, hasKey, lastPolledAt); dismissible setup notices (no AI key / server never connected → dashboard link) replace silent failure; envelope-code handling (RATE_LIMITED→"wait a minute", PLAN_INACTIVE→plan-card link, BAD_INPUT vs INTERNAL split); mic gets distinct translated no-speech/denied/unsupported states (feed messages, no alert() popups); brand favicon. All strings EN+AR.
- **M2_POLISH M4+M5 (backend): hardening + real test suite** — every endpoint now rides one spine (`lib/http.js endpoint()`): consistent error envelope `{ok:false,error:{code,message}}` with 10 documented codes, security headers + `x-request-id` on all responses, structured single-line JSON logs (no keys/tokens ever), 64KB body cap (413), same-origin CORS by default (`ALLOWED_ORIGIN` env opt-in), detailed secret-free `/api/health`. Firestore-backed per-tenant rate limit on `/api/command` (`RATE_LIMIT_PER_MIN`, default 30 — survives serverless). New tenant fields: throttled `lastPolledAt` (bridge liveness, ≤1 write/min) and `firstCommandAt` — both in `/api/tenant/me` for the dashboard checklist. `npm test`: 44 tests via node:test on the emulators — auth (incl. refresh path), pay-gate full cycle (deactivate→402→reactivate→re-block, ack un-gated), key encryption (round-trip + response-body leak scan), interpretation AR/EN/diacritics/Arabic-Indic digits, **rogue-provider whitelist proof** (test-only `providers/fake.js` returns forbidden actions; gate neutralizes them — with a live-control test so the proof can't pass vacuously), queue lifecycle, ask mode, envelope codes, rate limiting. TDD: red runs verified before each implementation (deviation: lastPolledAt/firstCommandAt were coded with the endpoint rewrite before their tests ran red — tests cover the throttle + none-doesn't-count edges). Old smoke test still green (15/15).
- **New M2 site+console shipped; open access (no payment); mobile-verified** — replaced `app/index.html` with the new one-page marketing site + auth + console from Downloads, set `LOCAL_PREVIEW=false`. The shipped file's login was wired to a non-existent `/auth/login` and expected the backend to mint tokens; re-wired it to our Firebase Auth (signup → `/api/signup` → Firebase signIn → `/api/tenant/me`), so it actually works against the real backend. Removed the payment barrier: `api/signup.js` now creates tenants `active:true` (the 402 gate + `subscriptionStatus` stay as the dormant Stripe seam). Why: the owner wants everything working with no payment, on mobile. Updated `scripts/smoke-emulator.js` to the active-by-default flow (kept the deactivation→402 check to prove the gate still works). Evidence: `npm run smoke:emulator` 15/15; real-browser E2E at 390×844 — signup → "Connected" (not locked) → «ابغى سيارة شرطة» → queued to Firestore (`spawn_vehicle{police}`), dev-server log confirms `active:true (open access)`.
- **First production tenant activated (m2-gaming)** — the owner created auth user `nettt420@gmail.com` in the Firebase console; its tenant doc was created via the Admin SDK: name "M2 Gaming", `active:true`, all six actions, bridge token generated. Why: console-created users don't get the tenant doc that `/api/signup` normally creates. Note: deploying `firestore.rules` via the service account failed (403 on serviceusage — the adminsdk SA can't drive the deploy API); rules must be pasted in the Firebase console → Firestore → Rules → Publish. UNTIL THAT'S DONE, if the DB was created in test mode it is publicly readable.
- **README "Live showcase" link added** — placeholder at the top of the README that becomes the one-click rendered preview of `docs/index.html` once the Vercel URL exists. Why: GitHub renders Markdown but never HTML; a hosted URL is the only way to preview the showcase from the repo page.
- **Repo identity fixed to Hustle-Forever only** — the machine's global git identity (42-school account) had authored all commits, showing a second GitHub account on the repo. Set a repo-local `user.name`/`user.email` (Hustle-Forever noreply) and rewrote history so every commit is authored by Hustle-Forever; force-pushed. Why: this project must be connected to the Hustle-Forever account exclusively. Other projects on this machine keep the global identity.
- **Original showcase restored** — the real `M2_Showcase.html` appeared in the project root (it wasn't on disk during the earlier search) and was moved to `docs/index.html`, replacing the generated stand-in. Why: the user's own showcase is the canonical front door; moved verbatim, render-verified. Note for later: its docs cards are not yet hyperlinked to the `.md` files.
- **Docs system created** — `docs/` with GOAL / ENGINEERING / DATABASE / SECURITY / FRONTEND / UIUX / AUTH / DAILY_PROGRESS, each seeded with the current state of its area, plus the showcase page at `docs/index.html`. Why: single place for project truth, and the standing docs-discipline rule starts today. Note: `M2_Showcase.html` was not found anywhere on the machine, so `docs/index.html` was **generated fresh** in the M2 design language as a stand-in — replace it with the original if it turns up.
- **GitHub prep** — fresh git repo rooted at the project (separate from the Desktop-wide repo), first commit `a7de38c` (52 files, no secrets — verified). `.gitignore` hardened with `*firebase-adminsdk*.json` / `service-account*.json` patterns. Why: pushing to a new private repo `Hustle-Forever/My_Gaming_Project_2026`; push itself is pending the owner's GitHub token.
- **Firebase wired to the real project** — the public `m2-gaming` web config went into `app/firebase-config.js` (`useEmulator:false`). Why: the deployed frontend must talk to the customer-facing Firebase project.
- **M2 platform completed (P1–P5)** — data layer + AES-GCM crypto, Firebase Auth (signup/verify + real login in both pages), Gemini forced-function provider with keyword fallback (default model `gemini-3.6-flash` after doc verification showed 2.5-flash retiring), Firestore queue with 402 pay-gate on command+poll, owner dashboard, `vercel.json`, Stripe seam stub. Why: PLATFORM_BUILD.md. Evidence: `npm run smoke:emulator` 16/16; browser E2E (signup → key save → activate → Arabic command queued through the real UI); legacy demo regression 7/7.

## 2026-08-09

- **Single-tenant demo built end-to-end (BUILD_TASK.md M1–M7)** — Express backend with closed six-action whitelist, Claude forced-tool interpretation (BYOK) + deterministic Arabic keyword stub, in-memory per-tenant queue, RTL Arabic web app with ar-AE voice input, FiveM Lua bridge (outward polling, named handlers only), mock bridge, smoke test. Why: prove the voice→AI→game pipe before building the SaaS layer. Evidence: `backend/npm run smoke` 7/7; live browser test of the Arabic flow.
