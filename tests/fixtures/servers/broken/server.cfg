# Deliberately broken fixture
sv_hostname "Broken RP"
ensure qb-inventory
ensure qb-core
ensure oxmysql
ensure ox_inventory
ensure qb-heists
ensure mx-nested
ensure bad-script
ensure hot-loop
ensure sketchy
ensure streams-heavy
set api_password "SuperSecret123"
set mysql_connection_string "server=localhost;uid=root;password=root123;database=qb"
